# Public asset path. For root-domain deploys leave as "/".
# For GitHub Pages at https://<user>.github.io/bombie/ use "/bombie/".
PUBLIC_URL_PATH=/

# Dev server settings
DEV_SERVER_PORT=8080

# Where webpack-dev-server proxies /api/* to (development only).
API_PROXY_TARGET=http://localhost:3001
